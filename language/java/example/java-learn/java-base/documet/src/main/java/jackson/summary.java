package jackson;

/**
 * @author fwz
 * @date 2019-03-15 21:01
 * @desc // 读取XML
 *       XmlMapper mapper = new XmlMapper();
 *       Book book = mapper.readValue(xml, Book.class);
 *       // 写入XML
 *       XmlMapper mapper = new XmlMapper();
 *       String xml = mapper.writeValueAsString(book);
 */
public class summary {

}
