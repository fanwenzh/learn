package tree;

/**
 * @author fwz
 * @date 2019-05-03 22:29
 * @desc
 */
public class depthFirstSearch {
    public static void main(String[] args) {
        // stack.push(root)
        //    while (stack is not empty):
        //        n = stack.pop()
        //        process(n) // "visit"
        //        for (each child of n, starting with the last one):
        //            stack.push(child)
    }
}
