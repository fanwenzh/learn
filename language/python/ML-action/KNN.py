import numpy as np
import operator

# 从大到小排序
classCount = {}
sortedClassCount = sorted(classCount.items(), key=operator.itemgetter(1), reverse='True')

# 文件数据解析
line = line.strip()
listFromLine = line.split('\t')
returnMat[index, :] = listFromLine[0:3]
classLabelVector.append(int(listFromLint[-1])

# 每一列最小值
minVals = dataSet.min(0) # axis = 0

# 唯一并集
vocabSet = set([])
vocabSet = vocabSet | set(othervacab)

