```java
// list, set, map, properties

// 堆栈 Stack
Stack<integer> st;
st.empty();
st.peek(); // 返回栈顶元素
st.pop(); 
st.push(el);
st.search(el);

// 链表 LinkedList
LinkedList<String> lList = new LinkedList<String>();
lList.add(E e); // .add(int index, E element);
lList.addFirst(e);
lList.addLast(e);
lList.removeFirst();
lList.removeLast();
lList.clear();
lList.getFirst();
lList.getLast();
lList.indexOf(e);
lList.lastIndexOf(e);
lList.set(index, e);

// vector
import java.util.Vector;

// Collections
import java.util.Collections;
Object obj1 = Collections.max(v);
Object obj2 = Collections.min(v);
Collections.swap(v, 0, 4); // index: 0, 4

// Queue
import java.util.Queue;
Queue<String> queue = new LinkedList<String>();
queue.offer("a"); // //添加元素


```